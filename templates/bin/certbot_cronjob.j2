#!/bin/bash

# Wildcards will not work without DNS interaction.
#certbot certonly -q --webroot -m 'aaron@bugher.net' --agree-tos --no-eff-email -d '{{ domain }}' -d '*.{{ domain }}'{% for domain_alias in domain_aliases %} -d '{{ domain_alias }}' -d '*.{{ domain_alias }}'{% endfor %}
